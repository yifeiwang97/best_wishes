# 优化后的依赖文件 - 解决编译问题

# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# 异步文件处理
aiofiles==23.2.1

# PyTorch（先安装，不包含 CUDA）
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0
torchaudio==2.1.0

# 音频处理
pydub==0.25.1

# TTS 核心（使用稳定版本）
# 先安装 numpy 避免版本冲突
numpy<2.0.0

# 预先安装兼容的 numba 和 llvmlite（使用预编译版本）
# 注意：如果这些还是失败，我们会跳过它们
llvmlite==0.40.1
numba==0.57.1

# TTS 及其依赖
TTS==0.22.0
