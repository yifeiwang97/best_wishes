# 语音克隆应用 - 环境变量配置示例
# 使用方法：复制此文件为 .env 并根据需要修改配置

# ========== 基础配置 ==========

# Coqui TTS 服务条款同意（必需设置为 1）
COQUI_TOS_AGREED=1

# 服务端口（默认 8000）
PORT=8000

# 主机地址（默认 0.0.0.0，允许外部访问；使用 127.0.0.1 仅本地访问）
HOST=0.0.0.0

# ========== 日志配置 ==========

# 日志级别：DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# ========== 路径配置 ==========

# 模型缓存路径（可选，留空使用系统默认：~/.local/share/tts）
# MODEL_CACHE_DIR=/path/to/model/cache

# 上传文件临时目录
# UPLOAD_DIR=./uploads

# 生成音频输出目录
# OUTPUT_DIR=./outputs

# ========== 限制配置 ==========

# 上传文件最大大小（MB）
MAX_UPLOAD_SIZE=50

# 文本最大长度（字符）
MAX_TEXT_LENGTH=5000

# 音频文件最小时长（秒）
MIN_AUDIO_DURATION=1

# 音频文件最大时长（秒）
MAX_AUDIO_DURATION=60

# ========== 维护配置 ==========

# 临时文件自动清理时间（小时）
CLEANUP_HOURS=24

# ========== 性能配置 ==========

# 设备选择（auto | cuda | mps | cpu）
# auto: 自动检测（推荐）
# cuda: 强制使用 NVIDIA GPU
# mps: 强制使用 Apple Silicon GPU
# cpu: 强制使用 CPU
DEVICE=auto

# 工作线程数（建议设置为 CPU 核心数）
# WORKERS=4
